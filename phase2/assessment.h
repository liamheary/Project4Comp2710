#ifndef ASSESSMENT_H
#define ASSESSMENT_H

#include "../phase1/question.h"  // Include the question definitions from Phase1.
#include <string>
using std::string;

// Function to run the assessment (quiz) using the linked list.
void runAssessment(QuestionNode* head);

#endif // ASSESSMENT_H

